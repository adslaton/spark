### Create and add your device key to cfg/tokens.json

```
{
    accessToken:    'YOUR_ACCESS_TOKEN',
    deviceId:       'YOUR_DEVICE_ID'
}
```

Please create cfg/tokens.json and place your keys in that location. The file located at cfg/tokens.json is hidden via .gitignore.